def linear_search(arr, target):
    for i in range(len(arr)):
        if arr[i] == target:
            print("Element found at index", i)
            return
    print("Element not found in index")
arr=[10,20,30,40]
target=int(input("enter your target value"))
binary_search(arr,target)
